<div class="container p-4" >
    <div class="row">
        <div class="col-md-10 mx-auto">
            <div class="card text-center">
                <div class="card-body" align="left">
    

        <form action="/cases" method="POST" id="form_submit" >  
            <div class="row" >
                <div class="col">
                    <select class="select" name="search" id="search" onchange="generatedatafield();">
                            {{#each campos}} <option value={{this}}>{{this}}</option>{{/each}}
                    </select>
                </div>

                <div class="col">
                    <input class="search_input" id="search_input" name="search_input" type="search" />
                    <button class="btn btn-primary"> <i class="fas fa-search"></i></button>
                </div>
                    
                    <input type="hidden" name="table_name" id="table_name", value="advisers" class="form-control">                
            </div>
        </form>
        



<hr class="hr" />


                     <table class="table table-hover" id="tabletable">
                        <thead>
                             <tr><th> ID</th> <th>Status</th> <th>Fecha del caso</th> <th>Asesor</th><th>#Siniestro</th><th>Rut prop</th><th>Dirección</th></tr>
                       
                        </thead>
                        <tbody>
                        {{#each cases}}
                                <tr><td>#{{this.id_case}}</td><td>{{this.status_name}}</td><td>{{this.case_date}}</td>
                                <td>{{this.adviser_name }} &nbsp; {{this.adviser_lastname }}</td>
                                <td>#{{this.incident_code}}</td><td>{{this.client_rut}}</td><td>{{this.client_address}}</td></tr>
                        {{/each}}
                        </tbody>
                    </table>
                    <nav aria-label="...">
  <ul class="pagination">
    <li class="page-item ">
      <a class="page-link" href="/cases/?page={{#rest page}}{{/rest}}" tabindex="-1" aria-disabled="false">Previous</a>
    </li>
    <li class="page-item">
      <a class="page-link" href="/cases/?page={{#sum page numberOfPages}}{{/sum}}">Next</a>
    </li>
  </ul>
</nav>
                </div>
            </div>
        </div>

    </div>
</div>